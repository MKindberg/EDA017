public class RaceTurtleTest {
	public static void main(String[] args) {
		RaceWindow w = new RaceWindow();
		RaceTurtle r = new DizzyTurtle(w, 1, 3);

		while (r.getX() < RaceWindow.X_END_POS) {
			r.raceStep();
			RaceWindow.delay(10);
		}

		System.out.println("I mål!");
	}
}
